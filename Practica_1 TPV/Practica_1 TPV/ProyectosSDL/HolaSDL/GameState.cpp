#include "GameState.h"


//void GameState::handleEvent(SDL_Event event)
//{
//	auto it = stage.begin();
//	bool handled = false;
//	while (it != stage.end() && !handled) {
//		if ((*it)->handleEvent(event))
//			handled = true;
//		else ++it;
//	}
//}